## Phase 9 — Motion System Setup
- [ ] Install `framer-motion` and `aos`
- [ ] Create `src/lib/motion.ts` tokens
- [ ] Initialize AOS globally in `App.tsx`
- [ ] Add `MotionConfig reducedMotion="user"` to `App.tsx`
- [ ] Create `AnimatedRoutes.tsx` and wrap `Routes` with `AnimatePresence mode="wait"`

## Phase 10 — Landing Page Motion
- [ ] Apply Framer Motion to Hero (badge, H1, subhead, CTA)
- [ ] Apply continuous float loop to Ghost illustrations
- [ ] Add AOS `fade-up` to Stats, How It Works, Premium teaser, Footer
- [ ] Apply `staggerContainer` to Market Preview Strip
- [ ] Create number counter for Stats strip

## Phase 11 — App Shell Motion
- [ ] Add scale/hover transition to Wagmi Connect button in `AppNav`
- [ ] Ensure Toaster layout works

## Phase 12 — Markets Explorer Motion
- [ ] Add `staggerContainer` to grid
- [ ] Add `AnimatePresence` and `fadeUp` to MarketCards with `market.id` key
- [ ] Add `layoutId="tab-indicator"` to Filter tabs
- [ ] Add subtle hover lift to MarketCards

## Phase 13 — Market Detail Motion
- [ ] Animate pool split bar width
- [ ] Add `layoutId` pill to outcome radio buttons
- [ ] Add loading spinner/morph to Bet submission button
- [ ] Add `AnimatePresence` `fadeUp` to Claim button
- [ ] Add `layout` auto-height to PaywallModal
- [ ] Wrap Activity Feed in `layout` container

## Phase 14 — User Dashboard Motion
- [ ] Stagger Stat tiles and count-up
- [ ] Stagger History table rows
- [ ] Fade/scale empty state

## Phase 15 — Admin Dashboard Motion
- [ ] Minimal fast crossfades for Admin view
